# Anaconda configuration file for Fedora IoT.

[Profile]
# Define the profile.
profile_id = fedora-iot
base_profile = fedora

[Profile Detection]
# Match os-release values.
os_id = fedora
variant_id = iot

[Storage]
default_scheme = LVM
