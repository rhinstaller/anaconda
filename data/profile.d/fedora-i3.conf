# Anaconda configuration file for Fedora i3 Spin.

[Profile]
# Define the profile.
profile_id = fedora-i3
base_profile = fedora

[Profile Detection]
# Match os-release values.
os_id = fedora
variant_id = i3

[User Interface]
# Live spin default browser for the Web UI.
webui_web_engine = slitherer
