# Anaconda configuration file for Fedora KDE Plasma Mobile spin.

[Profile]
# Define the profile.
profile_id = fedora-kde-mobile
base_profile = fedora-kde

[Profile Detection]
# Match os-release values.
os_id = fedora
variant_id = kde-mobile

[Payload]
default_environment = kde-mobile-environment
