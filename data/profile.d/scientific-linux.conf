# Anaconda configuration file for Scientific Linux.

[Profile]
profile_id = scientific-linux
base_profile = rhel

[Profile Detection]
os_id = scientific

[Anaconda]
forbidden_modules =
    org.fedoraproject.Anaconda.Modules.Subscription

[Payload]
default_source = CLOSEST_MIRROR

updates_repositories =
    updates
    updates-modular
