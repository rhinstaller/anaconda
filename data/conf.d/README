# This is a drop directory for anaconda configuration files. Files are read in order of file names.
# This directory is meant for overrides, debugging, and other one-time uses.
# Prefer using regular, complete profile files in the "profile.d" directory if possible.
